/**
 * Test Fixtures - Entity Generators
 * 
 * Uses zod-fixture to generate realistic test data for entity types.
 * Import these in tests to quickly create valid entity instances.
 */
export { createMockProject, ProjectGenerator } from './projectFixture';
export { createMockTask, TaskGenerator } from './taskFixture';
